import asyncio
import datetime
import logging
import os
import random

from aiogram import Bot, Dispatcher, F, types
from aiogram.filters import Command, CommandStart
from aiogram.types import InlineKeyboardButton, InlineKeyboardMarkup, Message, CallbackQuery
from aiogram.fsm.storage.memory import MemoryStorage
from sqlalchemy import BigInteger, Boolean, Column, DateTime, Integer, String, select
from sqlalchemy.ext.asyncio import AsyncSession, async_sessionmaker, create_async_engine
from sqlalchemy.orm import DeclarativeBase, Mapped, mapped_column

logging.basicConfig(level=logging.INFO, format="%(asctime)s [%(levelname)s] %(name)s: %(message)s")
logger = logging.getLogger("VenomPredictionBot")

BOT_TOKEN = os.getenv("PREDICTION_BOT_TOKEN", "8872020288:AAHbHL2pcTjcNV6jlO7N-HdG8BbV0NfeEjk")
DB_URL = os.getenv("PREDICTION_DB_URL", "sqlite+aiosqlite:///./prediction_bot.db")

engine = create_async_engine(DB_URL, echo=False)
AsyncSessionLocal = async_sessionmaker(bind=engine, class_=AsyncSession, expire_on_commit=False)

class Base(DeclarativeBase):
    pass

class BettingChannel(Base):
    __tablename__ = "betting_channels"
    id: Mapped[int] = mapped_column(Integer, primary_key=True, autoincrement=True)
    channel_id: Mapped[int] = mapped_column(BigInteger, unique=True, nullable=False)
    channel_title: Mapped[str] = mapped_column(String(255), default="Channel")
    added_by: Mapped[int] = mapped_column(BigInteger, nullable=False)
    auto_post: Mapped[bool] = mapped_column(Boolean, default=True)

async def init_db():
    async with engine.begin() as conn:
        await conn.run_sync(Base.metadata.create_all)

# ---------------- Match Banker Generator ----------------

MATCH_DATABASE = [
    ("Arsenal vs Chelsea", "🏴󠁧󠁢󠁥󠁮󠁧󠁿 Premier League", "Home Win & Over 1.5", 1.85),
    ("Real Madrid vs Barcelona", "🇪🇸 La Liga", "Both Teams to Score (BTTS) & Over 2.5", 1.95),
    ("Bayern Munich vs Dortmund", "🇩🇪 Bundesliga", "Over 3.5 Goals", 2.10),
    ("Man City vs Liverpool", "🏴󠁧󠁢󠁥󠁮󠁧󠁿 Premier League", "Over 2.5 Goals", 1.65),
    ("PSG vs Marseille", "🇫🇷 Ligue 1", "Home Win & Over 2.5", 1.80),
    ("Inter Milan vs Juventus", "🇮🇹 Serie A", "Under 2.5 Goals", 1.75),
    ("Sporting CP vs Benfica", "🇵🇹 Primeira Liga", "Both Teams to Score (BTTS)", 1.70),
]

def generate_daily_banker_slip() -> str:
    match, league, tip, odds = random.choice(MATCH_DATABASE)
    confidence = random.choice(["⭐⭐⭐⭐⭐ (98% High Banker)", "⭐⭐⭐⭐ (88% Value Pick)"])

    sporty_code = f"BC{random.randint(10000, 99999)}"
    bet9ja_code = f"{random.randint(10, 99)}X{random.choice('ABCDEF')}{random.randint(100, 999)}"
    onexbet_code = f"{random.choice('XYZ')}{random.randint(1000, 9999)}"

    now_str = datetime.datetime.now(datetime.timezone.utc).strftime("%Y-%m-%d %H:%M UTC")

    slip_text = (
        f"⚽ <b>VENOM DAILY FOOTBALL BANKER SLIP</b> ⚽\n\n"
        f"🏆 <b>League:</b> {league}\n"
        f"⚔️ <b>Match:</b> <b>{match}</b>\n\n"
        f"🎯 <b>Prediction:</b> <code>{tip}</code>\n"
        f"📈 <b>Odds:</b> <b>{odds:.2f}</b>\n"
        f"🔥 <b>Confidence:</b> {confidence}\n\n"
        f"🎟️ <b>BOOKING CODES:</b>\n"
        f"• <b>SportyBet:</b> <code>{sporty_code}</code>\n"
        f"• <b>Bet9ja:</b> <code>{bet9ja_code}</code>\n"
        f"• <b>1xBet:</b> <code>{onexbet_code}</code>\n\n"
        f"🕒 <i>{now_str}</i>\n"
        f"🚀 <i>Auto-Generated by Venom Tech Predictions</i>"
    )
    return slip_text

# ---------------- Bot Handlers ----------------

bot = Bot(token=BOT_TOKEN)
dp = Dispatcher(storage=MemoryStorage())

@dp.message(CommandStart())
async def cmd_start(message: Message):
    text = (
        "⚽ <b>Welcome to VENOM FOOTBALL PREDICTIONS BOT</b>\n\n"
        "Your automated source for daily high-odds football bankers and booking codes.\n\n"
        "⚡ <b>Features:</b>\n"
        "• Daily Banker of the Day\n"
        "• Automatic SportyBet, Bet9ja & 1xBet Booking Codes\n"
        "• Auto-Post Bot for Telegram Betting Channels\n\n"
        "Tap below to generate today's banker slip:"
    )
    kb = InlineKeyboardMarkup(
        inline_keyboard=[
            [InlineKeyboardButton(text="🎯 Generate Today's Banker Slip", callback_data="gen_banker")],
            [InlineKeyboardButton(text="📢 Add to Betting Channel (Auto-Post)", callback_data="add_channel")],
        ]
    )
    await message.answer(text, reply_markup=kb, parse_mode="HTML")

@dp.callback_query(F.data == "gen_banker")
async def cb_gen_banker(callback: CallbackQuery):
    slip = generate_daily_banker_slip()
    kb = InlineKeyboardMarkup(
        inline_keyboard=[[InlineKeyboardButton(text="🔄 Generate Another Match", callback_data="gen_banker")]]
    )
    await callback.message.answer(slip, reply_markup=kb, parse_mode="HTML")
    await callback.answer()

@dp.callback_query(F.data == "add_channel")
async def cb_add_channel(callback: CallbackQuery):
    text = (
        "📢 <b>How to Auto-Post to your Betting Channel:</b>\n\n"
        "1. Open your Telegram Channel settings ➡️ <b>Administrators</b>.\n"
        "2. Add this bot as an Admin with <b>Post Messages</b> permission.\n"
        "3. Send <code>/link_channel</code> inside your channel.\n"
        "4. The bot will automatically drop daily match slips and booking codes to your subscribers!"
    )
    kb = InlineKeyboardMarkup(inline_keyboard=[[InlineKeyboardButton(text="« Back", callback_data="home")]])
    await callback.message.edit_text(text, reply_markup=kb, parse_mode="HTML")
    await callback.answer()

async def main():
    await init_db()
    logger.info("Venom Football Prediction Bot started...")
    await dp.start_polling(bot)

if __name__ == "__main__":
    asyncio.run(main())
